This directory contains the notices needed for the source tree and the
redistributable application. `setup_models.ps1` also copies the exact upstream
model license files beside the optional external GGUF assets.

Before commercial distribution, review the complete dependency tree and the
licenses of the Vulkan SDK/loader package actually selected by the build.
